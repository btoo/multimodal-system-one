# Frozen v4 panel fixtures

These project-generated images are the exact panel bytes used by the frozen v4 manifest. They avoid a font-dependent regeneration step. Each image hash is recorded in `evals/manifests/v4_selection_v1.jsonl`. Natural speech is fetched separately from the attributed public source.
